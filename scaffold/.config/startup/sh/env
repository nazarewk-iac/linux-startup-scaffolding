# TRIGGER: runs from ~/.profile
# WARNING: this will not be run for non-login, non-interactive shells.

for file in ~/.config/startup/sh/env.d/*.sh ; do
  if [ -r "${file}" ] ; then
    . "${file}"
  fi
done
